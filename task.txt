Исправь порядок выполнения кнопки Run в tools/upgr_agent_gui.py.

Проблема:
Сейчас auto-codex.ps1 запускается ДО git commit, из-за этого orchestrator всегда падает с "Repo dirty".

Требования:
1. При нажатии Run:
   - сначала сохранить текст в task.txt
   - затем выполнить последовательно:
     git add task.txt
     git commit -m "task: from GUI"
   - дождаться завершения git-команд
2. Только после успешного git commit запускать auto-codex.ps1
3. Если git commit не прошёл:
   - НЕ запускать orchestrator
   - вывести ошибку в лог
4. Весь вывод git и codex должен отображаться в live output
5. Не изменять другие файлы

После выполнения остановись.