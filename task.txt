Исправь auto-commit в tools/upgr_agent_gui.py.

Проблема:
Сейчас коммитится только task.txt, из-за этого новые файлы остаются untracked и orchestrator падает.

Требования:
1. При нажатии Run:
   - выполнять:
     git add .
     git commit -m "task: from GUI"
2. Коммит должен включать ВСЕ изменения (не только task.txt)
3. Только после этого запускать auto-codex.ps1
4. Ошибки git выводить в лог
5. Не изменять другие файлы

После выполнения остановись.