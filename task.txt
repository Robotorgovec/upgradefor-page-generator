Исправь auto-commit в tools/upgr_agent_gui.py.

Проблема:
Сейчас repo становится dirty, потому что коммитится не всё.

Требования:
1. При нажатии Run:
   - сначала сохранить task.txt
   - затем выполнить:
     git add .
     git commit -m "task: from GUI"
2. Коммит должен включать ВСЕ изменения
3. Только после этого запускать auto-codex.ps1
4. Если нет изменений:
   - не падать
   - просто продолжить
5. Ошибки git выводить в лог
6. Не изменять другие файлы

После выполнения остановиться.